apiVersion: v1

kind: Service

metadata:
  name: postgres
  namespace: devboard
  labels:
    app: devboard-postgres

spec:
  clusterIP: None

  selector:
    app: devboard-postgres

  ports:
    - port: 5432   # service 
      targetPort: 5432   # Pod port
      protocol: TCP