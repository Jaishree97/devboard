apiVersion: v1
kind: Service

metadata:
  name: devboard-backend-service
  namespace: devboard
  labels:
    app: devboard-backend

spec:
  type: ClusterIP

  selector:
    app: devboard-backend

  ports:
    - protocol: TCP
      port: 8080         # service 
      targetPort: 8080   # Pod port