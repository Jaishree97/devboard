apiVersion: v1
kind: Service
metadata:
  name: devboard-frontend-service
  namespace: devboard

spec:
  type: NodePort

  selector:
    app: devboard-frontend

  ports:
    - port: 4173
      targetPort: 4173
      nodePort: 30080
      protocol: TCP